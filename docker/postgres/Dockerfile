FROM postgres:17-alpine

ARG POSTGRESQL_DB
ENV POSTGRES_DB=${POSTGRESQL_DB:-test}

ARG POSTGRESQL_USER
ENV POSTGRES_USER=${POSTGRESQL_USER:-test}

ARG POSTGRESQL_PASSWORD
ENV POSTGRES_PASSWORD=${POSTGRESQL_PASSWORD}